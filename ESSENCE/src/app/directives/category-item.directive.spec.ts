import { CategoryItemDirective } from './category-item.directive';

describe('CategoryItemDirective', () => {
  it('should create an instance', () => {
    const directive = new CategoryItemDirective(null);
    expect(directive).toBeTruthy();
  });
});
