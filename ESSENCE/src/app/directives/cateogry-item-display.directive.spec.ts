import { CateogryItemDisplayDirective } from './cateogry-item-display.directive';

describe('CateogryItemDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new CateogryItemDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
