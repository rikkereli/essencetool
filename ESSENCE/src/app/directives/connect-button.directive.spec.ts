import { ConnectButtonDirective } from './connect-button.directive';

describe('ConnectButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ConnectButtonDirective();
    expect(directive).toBeTruthy();
  });
});
