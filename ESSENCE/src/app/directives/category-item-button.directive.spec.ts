import { CategoryItemButtonDirective } from './category-item-button.directive';

describe('CategoryItemButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new CategoryItemButtonDirective(null,null);
    expect(directive).toBeTruthy();
  });
});
