import { ToogleActivityDirective } from './toogle-activity.directive';

describe('ToogleActivityDirective', () => {
  it('should create an instance', () => {
    const directive = new ToogleActivityDirective();
    expect(directive).toBeTruthy();
  });
});
